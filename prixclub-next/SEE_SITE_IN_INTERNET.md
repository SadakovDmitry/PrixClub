# 🌐 Как увидеть сайт в интернете

## ❌ Текущая проблема

Вы загрузили файлы на сервер, но сайт **не работает**, потому что:
1. Сборка падает с ошибкой
2. Next.js сервер не запущен

## ✅ Что нужно сделать

### Шаг 1: Исправить ошибку сборки

**Проблема:** На сервере остался старый файл `app/page.tsx`, который конфликтует.

**Решение:**

```bash
ssh deploy@92.242.60.85
cd var/www/sites/PrixClub/prixclub-next

# Удалите конфликтующий файл
rm -f app/page.tsx

# Обновите с GitHub
git pull

# Проверьте, что файл удален
ls app/ | grep "page.tsx$"
# Не должно быть page.tsx (может быть page.tsx.backup - это нормально)

# Пересоберите проект
npm run build
```

### Шаг 2: Запустить сайт через PM2

```bash
# Установите PM2 (если еще не установлен)
sudo npm install -g pm2

# Запустите сайт
pm2 start npm --name prixclub -- start

# Сохраните конфигурацию
pm2 save

# Настройте автозапуск
pm2 startup
# Выполните команду, которую покажет PM2
```

### Шаг 3: Настроить Nginx (если еще не настроен)

```bash
# Создайте конфиг для вашего домена
sudo nano /etc/nginx/sites-available/testprix
```

**Вставьте это:**

```nginx
server {
    listen 80;
    server_name testprix.ru www.testprix.ru;

    location / {
        proxy_pass http://localhost:3000;
        proxy_http_version 1.1;
        proxy_set_header Upgrade $http_upgrade;
        proxy_set_header Connection 'upgrade';
        proxy_set_header Host $host;
        proxy_cache_bypass $http_upgrade;
    }
}
```

**Активируйте:**

```bash
sudo ln -s /etc/nginx/sites-available/testprix /etc/nginx/sites-enabled/
sudo nginx -t
sudo systemctl restart nginx
```

---

## 🎯 Готово! Проверьте сайт

Откройте в браузере:
```
http://testprix.ru
https://testprix.ru
http://testprix.ru/ru/
https://testprix.ru/ru/cases/
```

---

## ⚠️ Если не работает

### Проверка 1: PM2 запущен?

```bash
pm2 status
# Должно быть: prixclub online
```

Если не запущен:
```bash
pm2 start npm --name prixclub -- start
pm2 save
```

### Проверка 2: Сайт отвечает локально?

```bash
curl http://localhost:3000
# Должен показать HTML код
```

### Проверка 3: Nginx работает?

```bash
sudo systemctl status nginx
# Должно быть: active (running)
```

### Проверка 4: Порт 3000 открыт?

```bash
sudo lsof -i :3000
# Должен быть процесс node
```

---

## 📝 Быстрая проверка всего

```bash
# На сервере выполните:
pm2 status           # Статус сайта
curl localhost:3000  # Работает ли сайт
sudo nginx -t        # Правильный ли конфиг Nginx
pm2 logs prixclub    # Логи сайта
```

---

## 🔗 Правильные URL для вашего сайта

После того, как всё заработает:

```
Главная:          https://testprix.ru/ru/
О нас:            https://testprix.ru/ru/about/
Команда:          https://testprix.ru/ru/team/
Кейсы:            https://testprix.ru/ru/cases/
Услуги:           https://testprix.ru/ru/services/
Проект 1:         https://testprix.ru/ru/projects/1/
Проект 2:         https://testprix.ru/ru/projects/2/
```

---

## ✨ Итог

**Чтобы увидеть сайт в интернете, нужно:**

1. ✅ Соберите проект (`npm run build`)
2. ✅ Запустите через PM2 (`pm2 start`)
3. ✅ Настройте Nginx как proxy
4. ✅ Откройте `https://testprix.ru`

**Без этого сайт не будет доступен в интернете!**

